# R Client Dependencies
# Install these packages before running the pipeline

install.packages(c(
  "httr",       # For API requests
  "jsonlite",   # For JSON parsing
  "DBI",        # Database interface
  "RPostgres",  # PostgreSQL driver
  "dplyr"       # Data manipulation
))
